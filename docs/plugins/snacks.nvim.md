# `snacks.nvim`

Provides notifications, terminal helpers, and shared UI utilities.

- Configuration: [`ui.lua`](../../lua/plugins/ui.lua)

## Installation

1. Run `:Lazy sync` to install the Neovim plugin.
2. System dependency: A working shell; Git is needed for Git-related features.

## Use

Use the configured terminal and notification mappings.

