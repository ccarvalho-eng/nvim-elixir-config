# `toggleterm.nvim`

Provides managed terminal windows.

- Configuration: [`docker.lua`](../../lua/plugins/docker.lua)

## Installation

1. Run `:Lazy sync` to install the Neovim plugin.
2. System dependency: A working shell; no additional package is required.

## Use

It is installed as the terminal dependency for `lazydocker.nvim`.
