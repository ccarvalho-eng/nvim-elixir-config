# `trouble.nvim`

Displays diagnostics, symbols, and location lists.

- Configuration: [`diagnostics.lua`](../../lua/plugins/diagnostics.lua)

## Installation

1. Run `:Lazy sync` to install the Neovim plugin.
2. System dependency: None.

## Use

Use the `<leader>x` mappings plus the configured symbol and location-list mappings.

