# `nvim-cmp`

Provides the completion menu and completion behavior.

- Configuration: [`completion.lua`](../../lua/plugins/completion.lua)

## Installation

1. Run `:Lazy sync` to install the Neovim plugin.
2. System dependency: None; install language servers separately for LSP suggestions.

## Use

Use the configured completion keys while typing.

