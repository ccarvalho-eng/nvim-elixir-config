# `vim-test`

Runs project tests from Neovim.

- Configuration: [`test.lua`](../../lua/plugins/test.lua)

## Installation

1. Run `:Lazy sync` to install the Neovim plugin.
2. System dependency: Install the test runner required by the current project.

## Use

Use the configured nearest, file, suite, and last-test mappings.

