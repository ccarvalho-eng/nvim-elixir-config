# `lualine.nvim`

Provides the status line.

- Configuration: [`ui.lua`](../../lua/plugins/ui.lua)

## Installation

1. Run `:Lazy sync` to install the Neovim plugin.
2. System dependency: Recommended: `brew install --cask font-fira-code-nerd-font` for icons.

## Use

Loads automatically.

