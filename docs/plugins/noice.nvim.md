# `noice.nvim`

Improves messages, command-line UI, and notifications.

- Configuration: [`ui.lua`](../../lua/plugins/ui.lua)

## Installation

1. Run `:Lazy sync` to install the Neovim plugin.
2. System dependency: None.

## Use

Loads automatically; use `<leader>un` for notification history where configured.

