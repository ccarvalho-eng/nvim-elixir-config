# `lazygit.nvim`

Opens LazyGit inside Neovim.

- Configuration: [`git.lua`](../../lua/plugins/git.lua)

## Installation

1. Run `:Lazy sync` to install the Neovim plugin.
2. System dependency: `brew install lazygit`.

## Use

Open it with the configured LazyGit command or mapping.

