# `telescope-project.nvim`

Adds project switching to Telescope.

- Configuration: [`telescope.lua`](../../lua/plugins/telescope.lua)

## Installation

1. Run `:Lazy sync` to install the Neovim plugin.
2. System dependency: `brew install fd ripgrep`; Git projects also require Git.

## Use

Open the configured Telescope project picker.

